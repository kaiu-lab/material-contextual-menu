/** Simple coordinates to define where to display the menu in the viewport. */
export interface MenuPosition {

  /** Horizontal position, in pixels. */
  x: number;

  /** Vertical position, in pixels. */
  y: number;
}
