import { inject, TestBed } from '@angular/core/testing';
// import { KaiuMenuService } from '../src';

describe('MenuService', () => {

  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [
        // KaiuMenuService,
      ],
    });
  });

  it('TODO',
      inject([/*KaiuMenuService*/],
          (/*menuService: KaiuMenuService*/) => {
            expect(true).toBeTruthy();
          }),
  );

});
